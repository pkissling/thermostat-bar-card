export const CARD_VERSION = '0.0.1';
// fallback min temperature if not explicity declared
export const DEFAULT_MIN_TEMPERATURE = 15
// fallback max temperature if not explicity declared
export const DEFAULT_MAX_TEMPERATURE = 25
